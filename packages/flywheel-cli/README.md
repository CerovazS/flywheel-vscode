# flywheel-cli

**Phase-2 placeholder.** This package will house the standalone CLI/TUI for navigating Flywheel graphs from the terminal (ink + ascii-petgraph). Out of scope for the MVP — see the plan, §8.
